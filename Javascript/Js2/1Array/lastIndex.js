let numbers = [10, 20, 30, 40, 50]
/* for (var i = 0; i <= lastindexfind.length; i++) {
    if (i == lastindexfind.length) {
        console.log(`Last index is ${i - 1}`);
    }
}
 */
console.log(numbers.lastIndexOf(numbers[numbers.length - 1])) // last index is ..4
console.log(numbers.indexOf(40))                          //  40 is 3 of index...3
console.log(numbers[numbers.length - 2])                  // Lsta 2nd value 40